# envman

Portable terminal environment-variable manager

## Bootstrap

This repo was scaffolded with the `python-utility` bootstrap shape and `universal-agent-repo` agent profile under the user-preferred development root.

The planning handoff prompt is at:

```text
docs/prompts/bootstrap-planning-agent.md
```
